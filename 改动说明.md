# 改动说明

本文档保留 V1.0 研发阶段的系统优化与功能增强记录。当前正式发布口径见
[`docs/V1.0_RELEASE_NOTES.md`](docs/V1.0_RELEASE_NOTES.md)。基线提交 `a508f38`
在干净 worktree 中验证为 `765 passed, 2570 warnings`。

## 一、性能优化

### 1. 归一化映射预编译（`safechat_guard/normalization/normalizers/mapping.py`）

- **问题**：`TokenMappingNormalizer.normalize` 在每次请求中对拼音/缩写词表的
  每个词条重新执行 `re.compile`，并且每次调用都重新对整个映射表排序，
  位于主检测路径上，是全项目最大的性能热点。
- **改动**：正则模式改为**构造期一次性预编译**（`_ordered_patterns`），
  词表排序结果同样在构造期缓存（`_ordered_entries`），请求路径零重复计算。
- **兼容性**：保留 `_ordered_mapping()` / `_pattern_for()` 原有方法签名。

### 2. V3 证据提取器共享与扫描优化（`safechat_guard/action_router_v3.py`）

- **问题**：`_unsafe_intent` / `_evidence_score` 每次调用都新建
  `RiskEvidenceExtractorV3` 实例（该类无状态，实例化纯属浪费）；
  `has_local_pair` / `active_operation_terms` 用逐索引 `startswith` 扫描全文，
  复杂度 O(文本长度 × 词条数)。
- **改动**：
  - 增加模块级共享实例 `_EVIDENCE_EXTRACTOR`，路由器与静态方法共用；
  - 扫描改用 `str.find` 循环定位（`_term_spans`），语义完全等价。

### 3. 消除隐藏的重复语义推理（`safechat_guard/pipeline.py`、`semantic_classifier.py`）

- **问题**：生产路径中 `semantic_classifier.detect()` 内部已调用
  `score_text()` 做过一次推理；而决策解释服务在解释时因结果中缺少
  `semantic_explanation` 字段，每次都会**再跑一次语义模型推理**，
  得到的是"第二次独立推理"的分数，既浪费性能又可能与真实决策不一致。
- **改动**：
  - `_scan_text_layers` 从返回 3 元组改为 4 元组，附带语义分数详情；
  - `SemanticClassifier.detect` 新增可选参数 `scored`，接受已算好的分数，
    避免重复推理（保留原调用方式，签名探测自动兼容测试替身）；
  - `_filter_text` 返回结果新增 `semantic_explanation` 字段，
    解释服务现在消费的是**该请求真实决策时使用的证据**。

### 4. 地址正则提升为模块级（`safechat_guard/output_guard.py`）

- **问题**：`mask_sensitive_info` 内部每次调用都重新构造并编译一条很长的
  地址识别正则。
- **改动**：提升为模块级常量 `_ADDRESS_PATTERN`，仅编译一次。

## 二、健壮性与可观测性

### 5. 静默吞异常改为带原因的警告（`safechat_guard/pipeline.py`）

- **问题**：三处 `except Exception` 完全静默：路由器加载失败（仅设置错误码，
  原始异常丢失）、`llm.status()` 失败（静默置空）、审计日志写失败
  （警告信息不含原因）。安全产品的降级路径无可观测性，排障困难。
- **改动**：三处均补充携带异常摘要的 `RuntimeWarning`；
  "audit event could not be persisted" 警告文案保持兼容（追加原因后缀）。

### 6. HTTP 500 补日志（`api_server.py`）

- **问题**：所有未处理异常统一返回 500 但**不记录任何堆栈**，线上排障只能靠猜；
  同时访问日志被整体关闭。
- **改动**：`_send_internal_error` 现在通过 `LOGGER.exception` 记录完整堆栈
  （logger 名 `safechat.api`），对外仍只返回通用错误信息。

### 7. 模型注册表并发加锁（`safechat_guard/model_registry.py`）

- **问题**：`set_enabled` / `set_default` / `_store_check` 对状态文件执行
  "读-改-写"但完全没有锁，多会话并发（前端每会话新建注册表实例）存在
  丢更新竞态。
- **改动**：新增类级 `RLock`（`_state_lock`）保护全部状态变更操作；
  类级锁同时覆盖多个实例并发访问同一文件的场景。

### 8. 环境变量读取竞态修复（`safechat_guard/llm_client.py`）

- **问题**：`chat()` 中 `os.environ[self.api_key_env]` 直接索引，与
  `status()` 的校验之间存在竞态——环境变量在两者之间被移除会抛出
  不被捕获的裸 `KeyError`。
- **改动**：改为 `os.environ.get(...)` 并在缺失时显式抛出带说明的
  `LLMClientError`。

### 9. 词表加载失败不再静默（`safechat_guard/normalization/providers.py`）

- **问题**：`JsonMapProvider.load()` 在文件缺失、JSON 损坏、格式非法时
  一律静默返回空字典——对一个以词表对抗变体攻击的系统，这意味着
  **词库静默失效且无人知晓**。
- **改动**：三种失败场景均发出 `RuntimeWarning`，包含文件路径与原因。

### 10. 前端异常补日志（`frontend/phase2_app.py`）

- **问题**：页面级 `except Exception` 只显示异常类型名，不打印堆栈，
  所有渲染 bug 都变成同一句"当前管理数据无法读取"。
- **改动**：增加 `logging.getLogger("safechat.frontend").exception(...)`
  记录完整堆栈；错误提示附带异常详情。

### 11. 重复快照获取收敛（`frontend/phase2_app.py`）

- **问题**：同一次页面渲染内 `health.snapshot()` 被调用 2-3 次、
  `registry.snapshot()` 2-3 次（快照本身要重建 8 个组件状态，无缓存）。
- **改动**：`main()` 中每帧只获取一次 `health_snapshot`，
  "安全对话"页复用已有快照判断 `model_calls_allowed`。

## 三、消除重复实现与死代码

### 12. 内置规则动作策略三处合一

- **问题**：内置关键词/正则规则的展示用 action/risk_level 推导逻辑
  （`porn/violence → block`、`score>=80 → block`）在
  `rule_management_service.py` 与 `api_server.py` 中**重复了三份**，
  与真实运行时策略（可配置阈值 + ActionRouter）存在漂移风险。
- **改动**：在 `rule_management_service.py` 新增
  `builtin_keyword_policy()` / `builtin_regex_policy()` 作为唯一权威，
  API 服务与管理服务共同调用。

### 13. 删除死代码

- 删除 `frontend/management_console.py`（354 行整文件，`management_views.py`
  的旧版拷贝，全仓库零引用）；
- 删除 `safechat_guard/models.py` 中的 `FilterResult`（全仓库零引用）；
- 删除 `safechat_guard/pipeline.py` 中的死方法 `_risk_level`
  （OutputGuard 有自己的同名实现，此方法无调用方）。

## 四、功能增强

### 14. 多轮对话支持

- `pipeline.handle_chat` 新增 `history` 参数：
  - 校验规则：仅允许 user/assistant 角色、内容非空、最多保留最近 20 条
    （`MAX_HISTORY_TURNS`，超出自动截断），不合法抛 `ValueError`；
  - 有历史时以 OpenAI messages 列表调用模型
    （`[*history, {"role": "user", "content": 脱敏后的新消息}]`），
    无历史时保持原字符串调用，**完全向后兼容**；
  - 安全性：历史内容均为已通过双向检测的内容；高风险输入依旧直接拦截，
    历史与新消息都不会转发给模型；历史不写入审计日志。
- `frontend/adapter.py` 的 `analyze` 透传 `history` 参数
  （为 None 时不传，兼容既有调用方与测试桩）。
- `frontend/chat_app.py` 安全对话页自动携带当前会话最近 20 条历史。
- `api_server.py` 的 `POST /api/chat` 新增可选 `history` 字段，
  带完整校验（非法返回 422 `invalid_history`，超长返回 413）。
- 新增 `tests/test_multiturn_chat.py`（8 个用例）：历史转发格式、
  无历史兼容、脱敏后消息入上下文、非法输入报错、超限截断、适配器透传。

### 15. 会话管理（类 DeepSeek 侧边栏）

- 新增 `frontend/session_store.py`：本地会话持久化层
  - 存储于 `data/runtime/chat_sessions.json`（该目录已被 Git 忽略），
    对话记录不进版本库、不进审计日志；
  - 原子写入（临时文件 + fsync + `os.replace`），文件损坏自动恢复为空；
  - 消息白名单清洗（仅 user/assistant 及脱敏展示结果）、
    标题自动派生（首条用户消息截断 24 字）、
    容量上限（100 会话 × 200 消息）。
- `frontend/chat_app.py` 新增"对话记录"侧边栏：
  - 新建对话、历史会话切换、删除会话；
  - 列表按最近更新时间倒序，**最新聊天的会话自动置顶**；
  - 当前会话以 `●` 前缀 + 高亮样式标识且始终可见（超过 15 个会话时也不会被挤出）；
  - 启动时自动恢复最近一次会话，每次对话后自动保存。
- 测试隔离：`chat_app.SESSION_STORE_PATH` 可替换，前端 AppTest
  （`test_phase2_app.py`、`test_chat_app_acceptance_v1.py`）的隔离 fixture
  将会话存储指向临时目录，测试与用户真实会话数据完全隔离。
- 新增 `tests/test_chat_session_store.py`（10 个用例）。

### 16. 评测指标口径补强（`safechat_guard/evaluation_service.py`）

- **问题**：Macro F1 的类别集合完全由当前批次数据动态决定——某类别缺失时
  直接从平均中消失，指标被"抬高"，不同批次之间不可比。
- **改动**：保留原 `macro_f1` / `macro_recall`（参与类别口径）不变以兼容，
  新增固定核心五类（`normal/ad/porn/violence/sensitive`，常量
  `CORE_CATEGORY_LABELS`）口径的 `macro_f1_core` / `macro_recall_core`，
  并在指标中声明 `evaluated_labels` 与 `core_labels`，批次间可比。

## 五、文档

### 17. README.md 重写为最终发布版

- 重写为面向 GitHub 访客的完整文档：项目简介、六大核心功能
  （含多轮对话与会话管理）、快速开始（前端/API 双入口）、
  真实大模型接入指南（环境变量表 + 永久配置方法）、HTTP API 说明
  （含 history 请求示例）、目录结构、配置说明、安全设计、测试说明；
- 移除竞赛内部交付语境（成员分工、版本冻结哈希、评审口径等），
  只保留功能与使用方法。

### 18. 主要代码补充中文注释

为全部核心模块补充中文注释，分两个层次（仅注释，无任何逻辑变更）：

- **模块级文档字符串**：说明模块职责、设计意图与安全约束；
- **函数/方法级文档字符串**：每个关键函数、方法均说明用途、参数含义、
  返回结构与决策规则（含路由优先级、阈值含义、Fail-Closed 行为等）。

覆盖文件：

- 核心链路：`pipeline.py`（全部方法）、`normalizer.py`、`rule_filter.py`、
  `rule_manager.py`
- 语义与路由：`semantic_classifier.py`、`action_router_v3.py`、`output_guard.py`
- 模型接入：`llm_client.py`、`llm_adapters.py`、`model_registry.py`
- 服务与日志：`logger.py`、`api_server.py`、`evaluation_service.py`、
  `decision_explanation_service.py`、`analytics_service.py`、
  `audit_service.py`、`health_service.py`
- 前端：`chat_app.py`、`session_store.py`、`adapter.py`、`phase2_app.py`

## 六、测试

- 全量测试：**717 个用例全部通过**（优化前基线 699 个，
  新增多轮对话 8 个 + 会话存储 10 个）；
- 新增测试文件：
  - `tests/test_multiturn_chat.py`（多轮对话）；
  - `tests/test_chat_session_store.py`（会话持久化）；
- 兼容性处理：`_scan_text_layers` 签名变化与 `detect(scored=...)`
  新参数均通过签名探测自动兼容既有测试替身（EmptyDetector、lambda 桩等），
  未修改任何既有测试的断言（仅补充两处 fixture 的会话存储路径隔离）。

## 七、V1.0 Release Cleanup 后续边界

后续发布整理已将正式 Streamlit 入口收敛为薄壳，将九页实现命名为
`frontend/product_app.py`，并把 `LLMClientFactory` 明确为兼容层；Evaluation CSV
默认也已不包含原始 `text`。详情见 `docs/V1.0_RELEASE_NOTES.md`。

以下安全边界仍刻意留待单独的 security hardening 阶段：

1. 多轮 history 完整安全链增强；
2. 异常会话持久化安全增强。
