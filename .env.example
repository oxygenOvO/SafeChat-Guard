# Reference only. SafeChat-Guard does not automatically load this file.
# Set secrets in the process environment or a secret manager; never commit values.
DASHSCOPE_API_KEY=
DEEPSEEK_API_KEY=

# Optional: select the real-LLM example while keeping config.yaml on mock.
SAFECHAT_CONFIG_PATH=config.real_llm.example.yaml
