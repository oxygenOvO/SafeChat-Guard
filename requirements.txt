# The application baseline uses only the Python standard library.
# These packages are used only for training/loading the optional semantic model.
joblib>=1.1.0
pandas>=1.3.0
plotly==6.8.0
scikit-learn>=1.0.0
streamlit==1.58.0
